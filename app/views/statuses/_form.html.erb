<%= form_for(@status, html: {class: "form-horizontal"}) do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	
	<div class="form-group">
	
	<%= f.label :user_id %><br />
	<%= f.select(:user_id, User.all.collect{|p| [p.full_name, p.id]}, {}, { :class => "form-control"}) %></div>
	 
	 <div class="form-group"><%= f.label :context, "Content" %><br />
		<%= f.text_area :context, :class => "form-control" %></div>
	
  <div class="actions">
    <%= f.submit "Create Status", :class => "btn btn-primary"   %>
  </div>
<% end %>
